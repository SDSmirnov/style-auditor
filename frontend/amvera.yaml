meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile
  args:
    VITE_API_URL: https://amvera-olegkolokolnikov-run-auditor-b.amvera.io/api/analyze

run:
  containerPort: "80"
